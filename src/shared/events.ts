export enum CharacterEvents {
  FETCH_CLOTHING = 'character:fetchClothing',
  SEND_CLOTHING = 'character:sendClothing',
  FETCH_CHARACTERS = 'character:fetchCharacters',
  SEND_CHARACTERS = 'character:sendCharacters',
  SELECT_CHARACTER = 'character:selectCharacter',
  UPDATE_CLOTHING = 'chip-character:updateClothes',
}
